# Homepage Contact CTA Background

**Purpose**: Background image behind the final "準備好體驗下一代訓練方式了嗎？" CTA block, under a dark
gradient overlay + XR grid pattern (already implemented in `src/components/CTASection.astro`).

**Recommended ratio**: 16:9 (wide)
**Recommended size**: 1920×900 or wider

**Files**:
- `home-contact-cta.jpg`

Keep this image low-contrast/darker overall — text is overlaid directly on top with no separate
scrim beyond the built-in gradient.
