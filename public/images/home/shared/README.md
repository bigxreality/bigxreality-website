# Shared Homepage Assets

Reserved for images used across multiple homepage sections (e.g. a raster brand mark, if the
text-based logo in Header/Footer is ever replaced with an image). Currently empty — the header
and footer logo are rendered as styled text, not an image.
