# Homepage Package/Product Images (prepared, not yet rendered)

**Purpose**: Reserved for a future "套裝方案" homepage section. The current homepage does not render
a packages/product section (per the AIDA-structure spec, the homepage excludes product cards and
full product listings) — these files and the `packages` entry in `src/data/homepage-media.ts` are
prepared ahead of time so wiring the section in later doesn't require re-doing image plumbing.

**Recommended ratio**: 4:3 for lifestyle/scene shots, or product cut-out on transparent/white background
for a catalog-style shot (`object-fit: contain` in that case instead of `cover`).

**Files**:
- `home-package-sut.jpg` — SUT 小部隊戰術訓練系統
- `home-package-police-box.jpg` — POLICE BOX 攜帶式警勤訓練器
- `home-package-ai-military.jpg` — 軍事訓練套裝方案
- `home-package-uav.jpg` — 無人機戰術訓練模組
