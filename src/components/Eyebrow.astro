---
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<p
  class={`text-label font-bold uppercase tracking-[0.1em] text-text-muted mb-2 ${className}`}
>
  <slot />
</p>
