# 首頁圖片需求清單（舊版，已由 Astro 版本取代）

> **本文件已停用。** 這是 Next.js 版本（已淘汰）的圖片需求紀錄，保留作為歷史紀錄。
> 目前 Astro 版本的圖片需求清單請見專案根目錄 `/ASSETS_NEEDED.md`。

---

原內容（Next.js 版本，`/tw/home`，供對照）：

本清單對應 `lib/data/homepage-media.ts` 中的 `homepageMedia` 資料表與 `lib/data/homepage-content.ts` 中
`packages`（套裝方案）區塊。這兩個檔案已隨 Next.js 版本一併移除；Astro 版本改用
`src/content/siteSettings/*.json`、`src/content/solutions/*.md`、`src/content/caseStudies/*.md`
的 Content Collections 資料驅動圖片。
